name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  host-tests:
    # Fast, GPU-free lane that builds and runs the parser/host tests directly
    # with g++. Catches SQL tokenizer/parser and codegen regressions without
    # needing the CUDA toolkit.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run host-only parser tests
        run: ./tests/run_host_tests.sh

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build nvidia-cuda-toolkit python3-pip python3-dev
          python3 -m pip install --upgrade pip
          pip install pybind11 wheel
      - name: Configure
        run: cmake -S . -B build -G "Unix Makefiles"
      - name: Build
        run: cmake --build build -j $(nproc)
      - name: Build Python wheel
        run: pip wheel . -w dist --no-build-isolation
      - name: Run Python tests
        run: |
          pip install dist/*.whl
          python tests/test_python.py
      - name: Run tests
        run: |
          cd build
          ctest --output-on-failure
